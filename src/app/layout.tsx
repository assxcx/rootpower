import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "RootPower - Premium Plant Marketplace",
  description: "Discover rare and beautiful plants. Your trusted source for premium plants and botanical treasures.",
  keywords: "plants, rare plants, indoor plants, outdoor plants, plant marketplace, botanical",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
